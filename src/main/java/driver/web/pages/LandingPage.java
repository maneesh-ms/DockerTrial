package driver.web.pages;

import driver.web.general.CommonPageSection;
import org.openqa.selenium.WebDriver;

/**
 * The Home page which user lands on page load. Not much of this page is used in current tests.
 */
public class LandingPage extends CommonPageSection {

    /**
     * The page constructor
     *
     * @param driver
     */
    public LandingPage(WebDriver driver) {
        super(driver);
    }

    /**
     * Elements in Landing Page
     */
    //TODO while adding more tests, not necessary for current test flows

    /**
     * Actions in Page
     */
    //TODO while adding more tests, not necessary in current test flows

}
